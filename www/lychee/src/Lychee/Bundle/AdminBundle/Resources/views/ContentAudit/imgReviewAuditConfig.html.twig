{% extends 'LycheeAdminBundle:ContentAudit:layout.html.twig' %}

{% block body %}
    <form action="{{ path('lychee_admin_contentaudit_imgreviewauditconfig') }}" method="post" class="form-horizontal" >
        <div class="alert alert-danger" role="alert">这是一个危险操作!</div>

        <div class="form-group">
            <label for="pornSureRate" class="col-sm-2 control-label">确定色情阈值</label>
            <div class="col-sm-1">
                <input type="number" class="form-control" id="pornSureRate" min="0" name="config[1]" value="{{ config.1 }}" >
            </div>
            <small class="text-warning text-right" >范围为0~101，鉴黄结果为<mark>确定</mark>、<mark>色情</mark>并且概率>=该值，即自动删除图片</small>
        </div>
        <div class="form-group">
            <label for="pornUnSureRate" class="col-sm-2 control-label">疑似色情阈值</label>
            <div class="col-sm-1">
                <input type="number" id="pornUnSureRate" class="form-control" name="config[2]" min="0"  value="{{ config.2 }}" >
            </div>
            <small class="text-warning text-right" >范围为0~101，鉴黄结果为<mark>疑似</mark>、<mark>色情</mark>并且概率>=该值，即自动删除图片</small>
        </div>
        <div class="form-group">
            <label for="pornSureRate" class="col-sm-2 control-label">确定性感阈值</label>
            <div class="col-sm-1">
                <input type="number" class="form-control" id="sexySureRate" min="0" name="config[3]" value="{{ config.3 }}" >
            </div>
            <small class="text-warning text-right" >范围为0~101，鉴黄结果为<mark>确定</mark>、<mark>性感</mark>并且概率>=该值，即自动删除图片</small>
        </div>
        <div class="form-group">
            <label for="pornUnSureRate" class="col-sm-2 control-label">疑似性感阈值</label>
            <div class="col-sm-1">
                <input type="number" id="sexyUnSureRate" class="form-control" name="config[4]" min="0"  value="{{ config.4 }}" >
            </div>
            <small class="text-warning text-right" >范围为0~101，鉴黄结果为<mark>疑似</mark>、<mark>性感</mark>并且概率>=该值，即自动删除图片</small>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <button type="submit" class="btn btn-primary">保 存</button>
            </div>
        </div>
    </form>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function() {

        });
    </script>
{% endblock %}